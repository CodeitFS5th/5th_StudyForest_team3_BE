model Reaction {
  id      Int    @id @default(autoincrement())
  emoji   String
  count   Int    @default(0)
  studyId Int
  study   Study  @relation(fields: [studyId], references: [id])

  @@index([studyId])
}
